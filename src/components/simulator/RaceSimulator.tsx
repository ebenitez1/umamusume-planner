/**
 * STUB — owned by the Race Simulator module agent, which will overwrite
 * this file (and add inputs/results/comparison views) per ARCHITECTURE.md.
 */
export function RaceSimulator() {
  return (
    <section className="panel">
      <h2 className="panel-title">Race Simulator</h2>
      <p style={{ color: 'var(--muted)' }}>coming online…</p>
    </section>
  );
}
