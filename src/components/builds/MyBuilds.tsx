/**
 * STUB — owned by the Builds module agent (list/load/delete/share),
 * which will overwrite this file per ARCHITECTURE.md.
 */
export function MyBuilds() {
  return (
    <section className="panel">
      <h2 className="panel-title">My Builds</h2>
      <p style={{ color: 'var(--muted)' }}>coming online…</p>
    </section>
  );
}
