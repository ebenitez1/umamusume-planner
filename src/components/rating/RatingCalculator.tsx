/**
 * STUB — owned by the Rating Calculator module agent, which will overwrite
 * this file per ARCHITECTURE.md. Rendered as the optimizer tab's right rail.
 */
export function RatingCalculator() {
  return (
    <aside className="panel">
      <h2 className="panel-title">Rating Calculator</h2>
      <p style={{ color: 'var(--muted)' }}>coming online…</p>
    </aside>
  );
}
