/**
 * STUB — owned by the Skill IMPORT agent (manual entry / JSON import /
 * screenshot OCR / skill browser launcher), which will overwrite this file.
 */
export function SkillEntryTools() {
  return (
    <section className="panel">
      <h2 className="panel-title">Skill Entry</h2>
      <p style={{ color: 'var(--muted)' }}>coming online…</p>
    </section>
  );
}
