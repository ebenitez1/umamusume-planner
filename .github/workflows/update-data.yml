name: Refresh umapyoi catalog

on:
  schedule:
    # Weekly Mondays at 06:00 UTC. Adjust freely.
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Fetch latest catalog from umapyoi.net
        run: npm run fetch:data

      - name: Commit and push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet src/data/generated; then
            echo "No catalog changes."
            exit 0
          fi
          git add src/data/generated
          git commit -m "chore(data): weekly umapyoi catalog refresh"
          git push
